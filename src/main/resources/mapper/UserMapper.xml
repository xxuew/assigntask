<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wx.assigntask.dao.UserMapper">
  <resultMap id="BaseResultMap" type="com.wx.assigntask.entity.User">
    <!--
      WARNING - @mbg.generated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    <result column="user_id" jdbcType="INTEGER" property="user_id" />
    <result column="username" jdbcType="VARCHAR" property="username" />
    <result column="password" jdbcType="VARCHAR" property="password" />
    <result column="algo_id" jdbcType="INTEGER" property="algo_id" />
    <result column="frequency" jdbcType="INTEGER" property="frequency" />
    <result column="received_id" jdbcType="INTEGER" property="received_id" />
    <result column="total" jdbcType="INTEGER" property="total" />
  </resultMap>
  <insert id="insert" parameterType="com.wx.assigntask.entity.User">
    <!--
      WARNING - @mbg.generated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    insert into user (user_id, username, `password`, 
      algo_id, frequency, received_id, 
      total)
    values (#{user_id,jdbcType=INTEGER}, #{username,jdbcType=VARCHAR}, #{password,jdbcType=VARCHAR}, 
      #{algo_id,jdbcType=INTEGER}, #{frequency,jdbcType=INTEGER}, #{received_id,jdbcType=INTEGER}, 
      #{total,jdbcType=INTEGER})
  </insert>
  <select id="selectAll" resultMap="BaseResultMap">
    <!--
      WARNING - @mbg.generated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    select user_id, username, `password`, algo_id, frequency, received_id, total
    from user
  </select>
  <insert id="insertUser" keyProperty="user_id" parameterType="com.wx.assigntask.entity.User" useGeneratedKeys="true">
        insert into user (username,password) values (#{username},#{password})
    </insert>
  <select id="selectAllUser" resultType="com.wx.assigntask.entity.User">
        select * from user
    </select>
  <select id="findUserByUserName" resultType="com.wx.assigntask.entity.User">
        select * from user where username=#{username}
    </select>
  <select id="findUserById" resultType="com.wx.assigntask.entity.User">
        select * from user where user_id = #{user_id}
    </select>
  <select id="selectAllByTasking" resultType="com.wx.assigntask.entity.User">
        select * from user where tasking &lt; 3 order by tasking
    </select>

  <update id="updateTaking" parameterType="com.wx.assigntask.entity.User">
        update user set tasking = #{param2} where user_id = #{param1}
  </update>

  <update id="updateUser" parameterType="com.wx.assigntask.entity.User">
        update user set user_id= #{user_id}, username= #{username}, password= #{password}, algo_id= #{algo_id},frequency=#{frequency},received_id= #{received_id},total= #{total} where user_id = #{user_id}
  </update>
</mapper>