<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wx.assigntask.dao.UsertaskMapper">
  <resultMap id="BaseResultMap" type="com.wx.assigntask.entity.Usertask">
    <!--
      WARNING - @mbg.generated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    <result column="userId" jdbcType="INTEGER" property="userid" />
    <result column="subtaskid" jdbcType="INTEGER" property="subtaskid" />
    <result column="score1" jdbcType="VARCHAR" property="score1" />
    <result column="score2" jdbcType="VARCHAR" property="score2" />
  </resultMap>
  <insert id="insert" parameterType="com.wx.assigntask.entity.Usertask">
    <!--
      WARNING - @mbg.generated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    insert into usertask (userId, taskId, score1,
      score2)
    values (#{userid,jdbcType=INTEGER}, #{taskid,jdbcType=INTEGER}, #{score1,jdbcType=VARCHAR},
      #{score2,jdbcType=VARCHAR})
  </insert>
  <select id="selectAll" resultMap="BaseResultMap">
    <!--
      WARNING - @mbg.generated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    select userId, taskId, score1, score2
    from usertask
  </select>

  <select id="findByUserId" resultType="com.wx.assigntask.entity.Usertask">
    select * from usertask where userid = #{userid}
  </select>

  <insert id="assignTaskToU" parameterType="com.wx.assigntask.entity.Usertask">
    insert into usertask (userid,dividedid,subtaskid) values (#{userid},#{dividedid},#{subtaskid})
  </insert>
  <insert id="insertSubTask" parameterType="com.wx.assigntask.entity.Usertask">
    insert into usertask (userid,dividedid,subtaskid) values (#{userid},#{dividedid},#{subtaskid})
  </insert>

  <select id="selectAveScore1" resultType="int">
    select IFNULL(AVG(score1),0) as score1 from usertask where subtaskid=#{subtaskid}
  </select>
  <select id="selectAveScore2" resultType="int">
    select IFNULL(AVG(score2),0) as score2 from usertask where subtaskid=#{subtaskid}
  </select>
</mapper>